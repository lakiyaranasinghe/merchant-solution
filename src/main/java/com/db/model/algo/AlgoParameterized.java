package com.db.model.algo;

/*
    The AlgoGeneric interface is a contract for all the algorithm classes with perform action and multiple parameters
 */
public interface AlgoParameterized {
    void perform(int a, int b);
}
